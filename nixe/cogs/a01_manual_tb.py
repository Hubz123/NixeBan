from __future__ import annotations

# Intentionally register NOTHING to avoid duplicates.
async def setup(bot):  # type: ignore[override]
    return

def setup_legacy(bot):  # type: ignore[override]
    return
