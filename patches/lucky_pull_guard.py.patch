    --- a/nixe/cogs/lucky_pull_guard.py
    +++ b/nixe/cogs/lucky_pull_guard.py
    @@
-                line = yandere(user=user_mention, channel=channel_name, reason=reason)
+                line = yandere(user=user_mention, channel=channel_name, reason=reason)
+                # Tambah rujukan ke channel redirect agar user langsung tahu harus pindah ke mana.
+                try:
+                    from nixe.config.auto_defaults import cfg_str
+                    redirect = cfg_str("LUCKYPULL_REDIRECT_CHANNEL_ID", "")
+                except Exception:
+                    redirect = ""
+                if redirect:
+                    line = f"{line} → silakan lanjut di <#{redirect}>."
@@
-                    await msg.channel.send(line, delete_after=10)
+                    # Sedikit lebih lama supaya user sempat baca
+                    await msg.channel.send(line, delete_after=15)
